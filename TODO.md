# TODO

- [ ] Replace plaintext admin password in `src/utils/auth.js` with a hash-based check (frontend-only improvement).
- [ ] Update admin login message/UX if needed.
- [ ] Run `npm run lint` and `npm run build` to ensure no errors.
- [ ] Verify login flow works and protected routes unlock after successful login.

